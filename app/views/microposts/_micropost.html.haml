%li{:id => micropost.id}
  = link_to gravatar_for(micropost.use, size: 50), micropost.use
  %span.user= link_to micropost.use.name, micropost.use
  %span.content= micropost.content
  %span.timestamp
    Posted #{time_ago_in_words(micropost.created_at)} ago.
    - if current_use?(micropost.use)
      = link_to "delete", micropost, method: :delete, |
        data: { confirm: "You sure?" }                |